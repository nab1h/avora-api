المكتبات:
fortify لتسجيل الدخول والمستخدمين
sunctum للتحقق من المستخدم
spatie للصلاحيات 
socialite لتسجيل بحساب جوجل والفيس بوك




======================================
GET /api/users
لعرض جميع المستخدمين
GET /api/users?page=1
لعرض صفحة واحده وهي عباره عن 10
GET /api/users?search=ahmed
لعرض اي اسم او ايميل باسم ahmed
GET /api/users?page=2&per_page=10&search=ahmed
هيجيب أول 10 Users اللي الاسم أو الإيميل بتاعهم يحتوي على ahmed
GET /api/users?permission=create-products
المستخدمين اللي عندهم Permission اسمها create-products
GET /api/users?search=ahmed&role=admin
وتقدر تجمعهم
GET /api/users?sort=name
ترتيب بالاسم تصاعدي
GET /api/users?sort=-name
من الأكبر للأصغر / Z → A
GET /api/users?sort=-created_at
الأحدث أولًا
GET /api/users?sort=created_at
الأقدم أولًا
GET /api/users?search=ahmed&role=admin&sort=-created_at&page=1&per_page=5
وتقدر تجمع كل حاجة
=================================================
42|voV2aqU6WSggO0GKja0g7Kp5uxq98kkamIxm9JaPc9de0b3c


ahmeddd@example.com
NeswPassword1234


  "password": "ndA5edm2K6YkCm@s",

اصلاح عرض الافاتر
اظهار اعدادات الروفايل 
عمل كراد اليوزر
كراد الصلاحيات 
كراد الرول 

==========================
{isSuccess && (
            <p className="text-sm text-green-500">
              Password changed successfully
            </p>
          )}


==========================
{isLoading ? (
            <Button variant="secondary" disabled>
              Changing...
              <Spinner data-icon="inline-start" />
            </Button>
          ) : (
            <Button type="submit">
              Change password
            </Button>
          )}
==========================


"use client";

import type { ReactNode } from "react";
import { ProtectedRoute } from "@/components/protected-route";

export default function ProtectedLayout({ children }: { children: ReactNode }) {
  return <ProtectedRoute>{children}</ProtectedRoute>;
}
26911221602251
JT2038892
01284848505
7sSW%nrS2$YntQ$